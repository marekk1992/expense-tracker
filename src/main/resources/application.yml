spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/postgres?currentSchema=user_directory
      username: user
      password: postgres
  jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
